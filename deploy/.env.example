# Copy to repo-root .env (gitignored) and fill in. Consumed by docker-compose.

# --- Runtime ---
PORT=8787
ENVIRONMENT=production

# --- Caddy ---
# Public hostname for the API; Caddy issues a TLS cert for it automatically.
# (The web frontend is hosted on Cloudflare Pages, not on this box.)
API_DOMAIN=api.yourstudio.in

# --- Postgres (self-hosted, the db service) ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me-superuser-password
POSTGRES_DB=ipc
# The unprivileged login role the API connects as (SET ROLEs per request).
# Keep it alphanumeric — it is interpolated into SQL + the DATABASE_URL.
DB_AUTHENTICATOR_PASSWORD=change-me-authenticator-password

# --- Auth ---
# HS256 signing secret for app JWTs (was GoTrue). Use a long random string.
JWT_SECRET=generate-a-long-random-secret
# Google OAuth Client ID for "Continue with Google" (GIS id_token). Optional — login stays email+password if empty.
# Create at console.cloud.google.com → APIs & Services → Credentials → OAuth client ID (Web)
# Origins: https://your-worker.dev, http://localhost:5173
GOOGLE_CLIENT_ID=

# --- Email verification (Resend) ---
# Required in production: sign-in is refused until the user verifies their email.
# RESEND_API_KEY from resend.com; EMAIL_FROM a verified sender; APP_URL is the WEB
# app origin (where /verify lives) used to build the link.
RESEND_API_KEY=
EMAIL_FROM=IPC Studios <noreply@yourstudio.in>
APP_URL=https://app.yourstudio.in
# Testing without email set up: set ENVIRONMENT to anything but "production" and
# /auth/register returns the verification_token in its response so you can verify
# manually (POST /auth/verify or open APP_URL/verify?token=...).

# --- CORS ---
# Comma-separated web origins allowed to call the API (no trailing slash).
# Must include the Cloudflare Pages URL (e.g. https://ipc-studios.pages.dev and/
# or your custom app domain).
ALLOWED_ORIGINS=https://app.yourstudio.in

# --- Razorpay ---
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# --- Backups (the `backup` service) ---
# A nightly pg_dump lands in the db_backups volume regardless. Set the S3 block
# to also copy it OFF the box — a backup that only exists on the same VPS as the
# database does not survive losing the VPS.
BACKUP_AT_UTC=02:30
BACKUP_KEEP_DAYS=7
BACKUP_OFFSITE_KEEP_DAYS=30
# Any S3-compatible bucket. Cloudflare R2 example:
#   BACKUP_S3_PROVIDER=Cloudflare
#   BACKUP_S3_ENDPOINT=https://<account-id>.r2.cloudflarestorage.com
# Backblaze B2 / AWS / MinIO work the same way (set PROVIDER + ENDPOINT).
# Use a scoped key that can write and list this bucket only.
BACKUP_S3_BUCKET=
BACKUP_S3_PREFIX=ipc
BACKUP_S3_PROVIDER=
BACKUP_S3_ENDPOINT=
BACKUP_S3_REGION=auto
BACKUP_S3_ACCESS_KEY_ID=
BACKUP_S3_SECRET_ACCESS_KEY=

# --- Cron shared secret (x-cron-secret on /cron/reminders + /cron/attendance) ---
CRON_SECRET=generate-a-long-random-string

# --- Meta lead ads ---
# The verify token you type into Meta's webhook subscription form; the API
# refuses the GET handshake unless it matches.
META_VERIFY_TOKEN=

# --- Observability ---
# Header carrying the real client address, set by the proxy in front of the
# API. Caddy/nginx: X-Forwarded-For (default). Behind Cloudflare: CF-Connecting-IP.
CLIENT_IP_HEADER=X-Forwarded-For
# Sentry DSN for the API. Empty = errors are written to the structured log
# only, and no traces, performance data or cron check-ins are sent.
#
# This is the DSN, NOT an auth token. Sentry has three kinds of credential and
# only one of them belongs here:
#
#   DSN ................. goes here, and in the web build. Safe to ship; it can
#                         only submit events to its own project.
#   Organization token .. CI only, for uploading source maps at build time.
#                         Never needed at runtime, so never put it in this file.
#   Personal token ...... do not use for automation. It carries one person's
#                         access and dies with their account.
SENTRY_DSN=
# 0..1 — share of requests traced. Default 0.1 in production, 1 elsewhere.
# Set to 0 to keep error reporting but switch performance tracing off.
SENTRY_TRACES_SAMPLE_RATE=
# debug | info | warn | error
LOG_LEVEL=info
# Release identifier shown by /health and attached to error reports. The deploy
# workflow sets this to the commit SHA; left empty it reports "dev", which is
# why /health could not tell you what was deployed.
APP_VERSION=

# --- Meta lead ads: real lead fetch + webhook signature ---
# App secret verifies X-Hub-Signature-256 on every Meta post; the page token
# fetches the lead's fields by leadgen_id. Both empty = generic JSON webhook only.
META_APP_SECRET=
META_PAGE_ACCESS_TOKEN=

# --- WhatsApp Cloud API (optional) ---
# With both set, "send template" delivers the message from the API instead of
# opening wa.me on the user's phone, and the messaging wallet's worker
# (/cron/messages, the cron-messages service) sends approved utility templates
# from the platform's number. Without them queued messages fail with
# "WhatsApp not configured" and are refunded.
WHATSAPP_PHONE_NUMBER_ID=
WHATSAPP_ACCESS_TOKEN=
# Verify token for the WhatsApp webhook (callback URL: <API>/webhooks/whatsapp).
# Empty = META_VERIFY_TOKEN is used. Posts are signed with META_APP_SECRET,
# which must be set in production or delivery receipts are ignored.
WHATSAPP_VERIFY_TOKEN=

# --- Twilio click-to-call (optional) ---
# With all three set, "Call" in a deal rings the agent's phone and bridges the
# lead. Without them the call is logged as one dialled by hand.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# --- Mailbox sync (optional) ---
# EMAIL_SYNC_PROVIDER=gmail or o365; EMAIL_SYNC_TOKEN is an OAuth bearer token
# for the studio mailbox (gmail.readonly / Mail.Read); EMAIL_SYNC_MAILBOX is its
# address, used to tell sent from received. See docs/RUNBOOK.md.
EMAIL_SYNC_PROVIDER=
EMAIL_SYNC_TOKEN=
EMAIL_SYNC_MAILBOX=

# --- Refresh-token cookie mode (optional, recommended in production) ---
# AUTH_COOKIE=1 keeps the 30-day refresh token in an HttpOnly cookie on the API
# origin, out of reach of any script. Use SAMESITE=none (with HTTPS) when the
# app and the API are on different sites; DOMAIN scopes the cookie to a parent.
# Cookie-presented refreshes are Origin-checked against ALLOWED_ORIGINS, so a
# forged cross-site POST cannot spend the session.
AUTH_COOKIE=
AUTH_COOKIE_SAMESITE=lax
AUTH_COOKIE_DOMAIN=
